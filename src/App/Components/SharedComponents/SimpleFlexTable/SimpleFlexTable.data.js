export const testData = {
  title: "A simple title",
  data: "A simple data",
};

export const firstConfigToTest = Array(3).fill(testData);

export const secondConfigToTest = Array(5).fill(testData);
